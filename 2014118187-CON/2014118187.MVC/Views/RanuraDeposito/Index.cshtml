@model IEnumerable<_2014118187_ENT.Entities.RanuraDeposito>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.ATM.DescripcionATM)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Cantidad)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ATMId)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.ATM.DescripcionATM)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Cantidad)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.ATMId)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.RanuraDepositoId }) |
            @Html.ActionLink("Details", "Details", new { id=item.RanuraDepositoId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.RanuraDepositoId })
        </td>
    </tr>
}

</table>
